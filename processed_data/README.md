# processed_data

Working folder for data derived from the raw sources.

The apps in this repository fetch everything live and hold it in memory, so
nothing is written here at the moment. Raw inputs belong in `raw_data/`, which
is git-ignored.
